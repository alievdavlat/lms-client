import React from 'react'

type Props = {}

const Loader = (props: Props) => {
  return (

    <div className='w-screen h-screen loader-bg flex items-center justify-center'>

    <div className="loader">Loading
    <span></span>
    </div>
  
  </div>
  )
}

export default Loader